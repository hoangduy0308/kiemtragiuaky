@model List<MyMvcApp.Models.HocPhan>
@{
    ViewData["Title"] = "Danh sách học phần";
}

<h2 class="mb-3">📚 Danh sách học phần</h2>

<table class="table table-bordered">
    <thead class="table-light">
        <tr>
            <th>Mã HP</th>
            <th>Tên học phần</th>
            <th>Số tín chỉ</th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var hp in Model)
        {
            <tr>
                <td>@hp.MaHP</td>
                <td>@hp.TenHP</td>
                <td>@hp.SoTinChi</td>
                <td>
                    <form asp-action="DangKy" method="post" asp-controller="HocPhan">
                        <input type="hidden" name="maHP" value="@hp.MaHP" />
                        <button type="submit" class="btn btn-sm btn-success">Đăng ký</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
